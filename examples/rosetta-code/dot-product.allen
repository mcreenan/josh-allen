fn dot_product(left: List<Int>, right: List<Int>) returns Int {
  mut total: Int = 0;
  for index in 0..length(left) {
    total += left[index] * right[index];
  }
  total
}

export fn main() returns Int {
  dot_product([1, 3, -5], [4, -2, -1])
}
