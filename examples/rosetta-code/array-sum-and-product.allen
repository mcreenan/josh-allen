fn sum_and_product(values: List<Int>) returns (Int, Int) {
  mut sum: Int = 0;
  mut product: Int = 1;
  for value in values {
    sum += value;
    product *= value;
  }
  (sum, product)
}

export fn main() returns (Int, Int) {
  sum_and_product([1, 2, 3, 4, 5])
}
