# This is a TOML comment, not ALLEN source.
[package]
name = "comments-comment-parity"
version = "0.1.0"
language = "^0.1"

# The compiler must leave TOML comment handling to the manifest loader.
[[entry]]
name = "main"
function = "src/main.allen::main"
input = "Void"
output = "Int"
